/**
 * @file app.config.ts
 * @description nuxt 应用配置
 * @since 0.0.1
 */

export default defineAppConfig({
  title: 'Hutao-Web',
  theme: {
    dark: true,
    colors: {
      primary: '#ff0000'
    }
  }
})
